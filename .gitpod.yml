image:
  file: .gitpod.Dockerfile
  
tasks:
  - init: yarn install
    command: yarn build
    
ports:
  - port: 8080
    onOpen: open-preview
 
tasks:
  - init: pip3 install -r requirements.txt
    command: python3 ping.py
